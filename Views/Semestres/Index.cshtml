@model IEnumerable<ProyectoMVC.Models.Semestres>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Numero)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FechaI)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FechaF)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Carreras.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Numero)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FechaI)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FechaF)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Carreras.Nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Semestre }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Semestre }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Semestre })
        </td>
    </tr>
}

</table>
