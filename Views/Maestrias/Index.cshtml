@model IEnumerable<ProyectoMVC.Models.Maestrias>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Modalidad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Duracion_Semestres)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Modalidad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Duracion_Semestres)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Maestria }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Maestria }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Maestria })
        </td>
    </tr>
}

</table>
